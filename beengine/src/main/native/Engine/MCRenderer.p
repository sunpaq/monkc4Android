mixing(void, updateScene, MC3DScene* scene);
mixing(void, drawScene, MC3DScene* scene);
mixing(MCDrawMode, getDrawMode, voida);
mixing(void, setDrawMode, MCDrawMode mode);
mixing(void, scissorAllScene, int x, int y, int width, int height);
